import React from "react";
import { HeaderContainer } from "./MainStyle";
import HeaderLinks from "../Components/LinkComponents/HeaderLinks";

export default () => {
  return (
    <HeaderContainer>
      <HeaderLinks />
    </HeaderContainer>
  );
};
